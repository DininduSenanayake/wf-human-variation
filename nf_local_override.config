process.executor = 'slurm' 

executor {
    name = 'slurm'
    cpus = 32
    queueSize = 50
    memory = "128 GB"
}

singularity {
  runOptions = "-B ${TMPDIR}"
}


trace {
    enabled = true
    overwrite = true
    fields = 'task_id,name,status,exit,realtime,%cpu,rss'
}