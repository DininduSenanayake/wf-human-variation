process.executor = 'slurm' 

executor {
    name = 'slurm'
    cpus = 16
    queueSize = 50
    memory = "64 GB"
}

singularity {
  runOptions = "-B ${TMPDIR}"
}


trace {
    enabled = true
    overwrite = true
    fields = 'task_id,name,status,exit,realtime,%cpu,rss'
}